# gBizINFO（経済産業省 法人情報 API）の有効化ガイド

`coverletterkit.com` の「不捏造」エンジンの強化源。ユーザーが「企業名」を入力すると、
コードが自動で gBizINFO を呼び、**本物の法人データ**（業種・事業概要・資本金・従業員数・
特許・補助金・認定・職場情報）を取得し、AI 生成の prompt に注入する。これで「御社にしか
通用しない志望動機」が実現できる。

> コード側は**すでに接続済み**。本ドキュメントは「あなたがトークンを取得して Vercel に
> 設定する」手順のみ。ホストは公式と一致（`https://api.info.gbiz.go.jp/hojin/v2/hojin`）を
> 確認済み。

---

## 一、gBizINFO とは（再確認）
- 経済産業省が運営する政府保有法人データ（500万社以上）。**完全無料・クレカ不要**。
- 情報提供 REST API v2 で法人基本情報＋活動情報（財務・特許・調達・補助金・認定表彰・職場情報）を取得可能。
- 利用には**事前の利用申請**（メールアドレスのみ）が必要。

## 二、利用申請の手順（あなたがやること）

1. **申請フォームを開く**
   👉 `https://info.gbiz.go.jp/hojin/various_registration/form`
   （Gビズインフォ公式トップ → 「APIの利用」→「利用申請を行う」と同じ先）

2. **フォーム入力**
   - 利用規約に同意（チェック）。
   - メールアドレスを入力（届く「Web API 利用申請完了」メールがトークンへの鍵になる）。
   - 必要に応じて組織名等を入力。送信。

3. **メールを確認**
   - 登録したメールに **「Web API 利用申請完了」** というメールが届く。
   - 本文内の **URL をクリック** → API トークン（長い文字列）が表示される。
   - 🔴 メモ：このトークンは Swagger テスト用の「デモトークン」**ではなく**、あなた専用の本番トークン。

4. **トークンをコピーして安全に保管**
   - 例：`xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`（40文字前後の英数字）。

## 三、Vercel に設定（本番で有効化）

Vercel プロジェクト `career-ai` → Settings → Environment Variables → **Add** 1 件：

| Key | Value | Type |
|---|---|---|
| `GBIZ_API_TOKEN` | 手順二-3 でコピーしたトークン | **Secret**（推奨） |

設定後 → **Redeploy**（Deployments 最新件の ⋯ → Redeploy）。

> コードは `GBIZ_TOKEN` が無い場合は自動で gBiz 取得をスキップ（ユーザー入力のみで生成）。
> トークンを入れた瞬間から、企業名入力時に自動取得が始まる。

## 四、本番で動いているか確認

1. `coverletterkit.com` を開く。
2. 企業名欄に「トヨタ自動車」など実在企業を入力 → 生成。
3. 生成結果の状態表示に **「公開情報から抽出しました（要確認）」**（または `contextSource: "gbiz"`）が出れば成功。
4. 出力テキストに「業種：E／事業概要：…／資本金：…」等の gBiz 実データが混ざっていることを目視確認。

## 五、レート制限・注意

- 公式FAQ・各種連携ドキュメントで「**10分間に6,000回超**で10分間制限」とされる。
  本サイトは 1 生成あたり企業 1 件につき基本 2～6 回呼ぶだけなので、通常運用では届かない。
  万が一制限に触れたら `gbizGet` は 5xx/429 を返し、コードはその企業の gBiz 注入をスキップして
  継続動作する（サイトは止まらない）。
- 取得データは公開法人情報のため、出力に「要確認」の注記を自動付与済み。
- ESG 等の一部項目は現在のフェッチ対象外（基本・特許・補助金・調達・認定・職場を取得）。
  必要なら `gbizResearch()` の並列取得リストを拡張可能。

## 六、トラブルシュート

| 症状 | 原因 / 対処 |
|---|---|
| 生成に gBiz データが混ざらない | `GBIZ_API_TOKEN` 未設定 or Redeploy 忘れ。環境変数と再デプロイを確認。 |
| 「401 invalid token」 | デモトークン使用 or コピー欠け。手順二-3 の専用トークンを再取得。 |
| 特定企業でデータが取れない | 非上場・個人企業等は gBiz に無い場合あり。正常な動作（スキップして継続）。 |

### 切り分けの手順（2026-09-21 追加）

デプロイ後は `/api/generate` のレスポンスに **`gbiz`** という診断オブジェクトが入ります。
これを見れば原因が一発で判ります（Vercel の Function ログにも `[gbiz] {…}` が出ます）。

| `gbiz.step` | 意味 | 対処 |
|---|---|---|
| `no-token` | `GBIZ_API_TOKEN` が未設定 | Vercel の環境変数を確認 → Redeploy |
| `search` + `error:"http-401"` | トークンが不正・失効 | 手順二-3 の専用トークンを再取得 |
| `search-empty` | 法人データに該当なし | **正常**（個人事業主・屋号など）。FAIL ログは出ません |
| `detail-empty` | 検索は当たったが基本情報が取れない | 上流の一時障害が多い。時間をおいて再確認 |
| `done` + `ok:true` | 正常に取得できている | — |

> 失敗時は `[gbiz][FAIL] HTTP <status> <url> body=…` がログに残ります。
> 以前はこのログが無く、失敗しても無言で `null` を返していたため
> 「機能が全滅しているのに誰も気づけない」状態でした。

### 既知の落とし穴（修正済み）

- **検索 URL の末尾スラッシュ**：`/hojin/v2/hojin/?name=…` はルートに一致せず
  **トークンが正しくても必ず 500** を返します（2026-09-21 修正済み）。
  正しい形は `/hojin/v2/hojin?name=…`。
  判定法は「**401=ルート有り（認証層到達）/ 500=ルート未マッチ**」で、存在しない
  ダミールート `/zzz` が 500 になるのと同じ挙動です。
- **curl で確認しないこと**：Windows の schannel が TLS 再ネゴシエーションで応答本文を
  取りこぼすため、curl では誤った結論（500）に達します。**Node の fetch で確認**してください。
