# gBizINFO（経済産業省 法人情報 API）の有効化ガイド

`coverletterkit.com` の「不捏造」エンジンの強化源。ユーザーが「企業名」を入力すると
**候補（法人名／所在地／法人番号）が表示され、本人が 1 社を選んで確認したうえで**、
コードが gBizINFO から**本物の法人データ**（業種・事業概要・資本金・従業員数・特許・
補助金・認定・職場情報）を取得し、AI 生成の prompt に注入する。これで「御社にしか
通用しない志望動機」が実現できる。

> ⚠️ **生成前に必ず確認を挟む（2026-09-21 変更 / P0-0）**
> 以前はサーバが企業名から自動で 1 社を引いていた。ところが gBizINFO が直った直後から
> **「同名の別法人」を引いてしまう**という新しい危険が生まれた。捏造（存在しない情報）は
> まだ疑えるが、**実在する別会社の実データ**は一見それらしく見えるため、ユーザーが
> 「法務省やデジタル庁との取引実績」を本物のミライテックの面接で語ってしまう。
> そこで **`search`（候補一覧）→ ユーザーが選択 → `detail`（確認）→ 生成** の順に変更し、
> **サーバ側は企業名での自動検索を一切しなくなった**（＝誤配線の経路そのものを消した）。
> 確認済みの法人番号は `/^[0-9]{13}$/` で検証してから URL に連結する（パス注入防止）。

> コード側は**すでに接続済み**。本ドキュメントは「あなたがトークンを取得して Vercel に
> 設定する」手順のみ。ホストは公式と一致（`https://api.info.gbiz.go.jp/hojin/v2/hojin`）を
> 確認済み。

---

## 一、gBizINFO とは（再確認）
- 経済産業省が運営する政府保有法人データ（500万社以上）。**完全無料・クレカ不要**。
- 情報提供 REST API v2 で法人基本情報＋活動情報（財務・特許・調達・補助金・認定表彰・職場情報）を取得可能。
- 利用には**事前の利用申請**（メールアドレスのみ）が必要。

## 二、利用申請の手順（あなたがやること）

1. **申請フォームを開く**
   👉 `https://info.gbiz.go.jp/hojin/various_registration/form`
   （Gビズインフォ公式トップ → 「APIの利用」→「利用申請を行う」と同じ先）

2. **フォーム入力**
   - 利用規約に同意（チェック）。
   - メールアドレスを入力（届く「Web API 利用申請完了」メールがトークンへの鍵になる）。
   - 必要に応じて組織名等を入力。送信。

3. **メールを確認**
   - 登録したメールに **「Web API 利用申請完了」** というメールが届く。
   - 本文内の **URL をクリック** → API トークン（長い文字列）が表示される。
   - 🔴 メモ：このトークンは Swagger テスト用の「デモトークン」**ではなく**、あなた専用の本番トークン。

4. **トークンをコピーして安全に保管**
   - 例：`xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`（40文字前後の英数字）。

## 三、Vercel に設定（本番で有効化）

Vercel プロジェクト `career-ai` → Settings → Environment Variables → **Add** 1 件：

| Key | Value | Type |
|---|---|---|
| `GBIZ_API_TOKEN` | 手順二-3 でコピーしたトークン | **Secret**（推奨） |

設定後 → **Redeploy**（Deployments 最新件の ⋯ → Redeploy）。

> コードは `GBIZ_API_TOKEN` が無い場合は自動で gBiz 取得をスキップ（ユーザー入力のみで生成）。
> トークンを入れた瞬間から、企業名を入力したときの**候補表示**が始まる（未設定でもエラーには
> ならず、フォームに「候補が使えないので入力情報だけで生成します」と出るだけ）。

## 四、本番で動いているか確認

確認は**2 段階**に分かれました。候補が出ないことと生成が壊れていることは別問題なので、
切り分けは `/api/company` を先に見ます。

### 四-1. 候補と確認（`/api/company`）
1. `coverletterkit.com` を開く。
2. 企業名欄に「トヨタ自動車」など実在企業を入力 → **フォーカスを外す**（blur で検索が走る）。
3. 「候補から選んでください」が出て、**法人名／所在地／業種／設立／法人番号**の付いた
   ラジオボタンが並べば成功。同名の別法人があれば複数出ます（それが出ることが正常です）。
4. 1 社を選ぶと **「✓ 確認済み」＋「生成に使う登録情報を見る（n 項目・出典：gBizINFO）」**
   が出る。ここに並ぶ内容が**そのまま prompt に入る情報**なので、目視で確認できます。
5. 「リストに無い／企業情報を使わずに生成する」を選んだ場合も生成は可能（`declined`）。

`/api/company` へ直接打つ場合（Node の fetch で）：

```js
const r = await fetch("https://www.coverletterkit.com/api/company", {
  method: "POST", headers: { "content-type": "application/json" },
  body: JSON.stringify({ action: "search", name: "トヨタ自動車" })
});
console.log(await r.json());   // { configured, ok, action:"search", candidates:[…], diag:{…} }
```

- 未設定でも **500 にはならず** `200 { configured:false, reason:"not-configured" }` を返します
  （UI が「入力情報だけで生成」に降格できるようにするため）。
- `{ action:"detail", corporateNumber:"4180301018772" }` で `matched / facts / note / diag` が返り、
  **これが生成前にユーザーへ見せる内容**です。
- 1 文字の企業名は `400`、`GET` は `405`、同一 IP 60 回/時で `429`。

### 四-2. 生成に反映されているか（`/api/generate`）
6. 生成を実行。
7. レスポンスの `companyContextUsed: true` と `gbiz.step: "done"`、
   出力に「業種：E／事業概要：…／資本金：…」等の gBiz 実データが混ざっていることを確認。
8. 確認せずに生成した場合は **`gbiz.step: "no-confirmation"`** になり、
   `companyContextUsed` は `false`・`notice` に「企業情報の自動取得は企業名の候補を確認した
   うえで有効になります」が入ります（**これは正常動作**。サインの無いまま勝手に
   他社データを混ぜない、という設計どおりの結果です）。

## 五、レート制限・注意

- 公式FAQ・各種連携ドキュメントで「**10分間に6,000回超**で10分間制限」とされる。
  本サイトは候補検索 1 回（確認時）＋ 生成 1 回あたり最大 6 回（詳細取得）呼ぶだけなので、
  通常運用では届かない。万が一制限に触れたら `gbizGet()` は 5xx/429 を返し、コードは
  その企業の gBiz 注入をスキップして継続動作する（サイトは止まらない）。
- **自前の流量制限**：`/api/company` に **同一 IP 60 回/時**のプロセス内スライディング窓を
  入れている。Serverless は防御として完全ではないが、外部 API のトークンを無コストで
  焚き続けられる事態を止めるのが目的（超過は `429` + ログ `[company][LIMIT]`）。
- 取得データは公開法人情報のため、出力に「確認のうえ選択した登録情報を使用」の注記
  （`note`）を自動付与済み。**「要確認」という表現は、ユーザーが既に確認した後では
  矛盾するので 2026-09-21 に変更**しました。
- ESG 等の一部項目は現在のフェッチ対象外（基本・特許・補助金・調達・認定・職場を取得）。
  これらの並列取得リストは `api/_lib/gbiz.mjs` の `gbizDetail()` にあります。

## 六、トラブルシュート

| 症状 | 原因 / 対処 |
|---|---|
| 企業名を入れても**候補が出ない** | ① `GBIZ_API_TOKEN` 未設定 or Redeploy 忘れ ② その社名が法人データに無い（個人事業主・屋号・新設法人）③ 上流障害。①なら環境変数を確認して再デプロイ、②③は**仕様どおり**入力情報だけで生成されます。 |
| 生成に gBiz データが混ざらない | 候補を**選択していない**（`gbiz.step: "no-confirmation"`）か、トークン未設定。フォームの「✓ 確認済み」表示を確認してください。 |
| 「401 invalid token」 | デモトークン使用 or コピー欠け。手順二-3 の専用トークンを再取得。 |
| 特定企業でデータが取れない | 非上場・個人企業等は gBiz に無い場合あり。正常な動作（スキップして継続）。 |
| 同名企業が複数出て選べない | **それが P0-0 の目的です。** 候補を減らすのではなく、所在地・法人番号を見てユーザーに選ばせます（自動で 1 社に絞ると誤配線が復活します）。 |

### 切り分けの手順（2026-09-21 追加）

診断は 2 つのエンドポイントに分かれています。**まず `/api/company`（候補が取れるか）、
次に `/api/generate`（生成に反映されたか）** の順で見てください。

`/api/generate` のレスポンスには **`gbiz`** という診断オブジェクトが入ります。
（Vercel の Function ログには `[gbiz] {…}` が出ます）

| `gbiz.step` | 意味 | 対処 |
|---|---|---|
| `no-token` | `GBIZ_API_TOKEN` が未設定 | Vercel の環境変数を確認 → Redeploy |
| `no-confirmation` | **確認していないまま生成した**（P0-0 以降の既定） | **正常**。フォームで候補を選べば `done` になります |
| `declined` | ユーザーが「企業情報を使わない」を選んだ | **正常**。`note` に「使用していません」が入ります |
| `search` + `error:"http-401"` | トークンが不正・失効 | 手順二-3 の専用トークンを再取得 |
| `search-empty` | 法人データに該当なし | **正常**（個人事業主・屋号など）。FAIL ログは出ません |
| `detail-empty` | 検索は当たったが基本情報が取れない | 上流の一時障害が多い。時間をおいて再確認 |
| `done` + `ok:true` | 正常に取得できている | — |

> 失敗時は `[gbiz][FAIL] HTTP <status> <url> body=…` がログに残ります。
> 以前はこのログが無く、失敗しても無言で `null` を返していたため
> 「機能が全滅しているのに誰も気づけない」状態でした。
> `/api/company` 側は `[company][search]` / `[company][detail]` / `[company][SKIP]` /
> `[company][LIMIT]` の 4 種類で、**どれも 500 を返しません**。

#### `gbiz.diag` の読み方

| フィールド | 意味 |
|---|---|
| `configured` | `GBIZ_API_TOKEN` が設定されているか |
| `ok` | **上流から実データが取れたか**（`search-done` / `done` で true） |
| `step` | どこで止まったか（上の表） |
| `status` | 上流 HTTP ステータス |
| `error` | 失敗の理由（`http-401` 等）。正常系では null |
| `hits` | 検索で返ってきた候補数 |

> 2026-09-21 に修正：`gbizSearch()` が成功時に `ok` を立てておらず、
> `hits:81` / `step:"search-done"` なのに `diag.ok:false` という**自己矛盾した診断**を
> 返していました（`gbizDetail()` 側は立てていた）。ログを見た人が誤診するので意味を統一。
> **`search-empty` は「正常だがデータ無し」なので `ok:false` のまま**です
> （ここを true にすると今度は「データが取れた」と誤読されます）。

### 既知の落とし穴（修正済み）

- **検索 URL の末尾スラッシュ**：`/hojin/v2/hojin/?name=…` はルートに一致せず
  **トークンが正しくても必ず 500** を返します（2026-09-21 修正済み）。
  正しい形は `/hojin/v2/hojin?name=…`。
  判定法は「**401=ルート有り（認証層到達）/ 500=ルート未マッチ**」で、存在しない
  ダミールート `/zzz` が 500 になるのと同じ挙動です。
- **curl で確認しないこと**：Windows の schannel が TLS 再ネゴシエーションで応答本文を
  取りこぼすため、curl では誤った結論（500）に達します。**Node の fetch で確認**してください。
- **`process.env` をモジュール読み込み時に固定しないこと**：`const GBIZ_TOKEN = process.env.…`
  と書くと「読み込み順が挙動を決める」状態になり、テストで偽陽性/偽陰性が出ます。
  `api/_lib/gbiz.mjs` は `function gbizToken() { return process.env.GBIZ_API_TOKEN || ""; }`
  と**呼び出し時に読む**形にしてあります。

## 七、テスト（ネットワーク不要）

| スクリプト | 内容 |
|---|---|
| `node test-p1-baseline.mjs` | gBizINFO の URL 組み立て・失敗の可観測性・捏造禁止規則の前置・自己申告字数の除去・截断検知。fetch を横取りして**実際にハンドラを走らせる**。スタブは実測した壊れ方（末尾スラッシュ→500）を再現するので、同じ改修をすると落ちます。**187 項目** |
| `node test-p0.mjs` | P0-0 の候補確認フロー（search/detail/未設定 200/短名 400/GET 405/不正な法人番号で **fetch 0 回**/実レート制限 429）、H1・Title・構造化データ、占位符の短ラベル化、去歧義、字数、モバイル、信任表示。**219 項目** |

